import { DateFormsValidator } from './date-forms-validator'

describe('DateFormsValidator', () => {
  it('should create an instance', () => {
    expect(new DateFormsValidator()).toBeTruthy()
  })
})
