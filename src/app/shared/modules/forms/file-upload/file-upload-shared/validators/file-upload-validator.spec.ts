import { FileUploadValidator } from './file-upload-validator'

describe('FileUploadValidator', () => {
  it('should create an instance', () => {
    expect(new FileUploadValidator()).toBeTruthy()
  })
})
